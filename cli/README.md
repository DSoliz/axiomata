# CLI

Command-line tool for working with Axiomata knowledge bases.

Depends on `parser` and `core`.

Planned commands:
- `axm check <dir>` — validate all `.axm` files in a knowledge base
- `axm index <dir>` — print the global index of all statements
- `axm query <id>` — look up a statement by ID and show its value and references
